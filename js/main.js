/**
* Add Navigation to Home Page as soon as the page is loaded
**/
document.addEventListener('DOMContentLoaded', () => {
    addNavigation();
})


/** 
* Adds navigation link to page
**/
addNavigation = () => {
    let navigationMenu = ['Home', 'About', 'Programs', 'Services', 'Testimonials', 'Contact'];
    
}


